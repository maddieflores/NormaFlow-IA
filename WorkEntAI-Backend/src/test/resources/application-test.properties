# Perfil de test: valores dummy para que el contexto arranque sin infraestructura real.
# MongoDB y Spring AI se configuran con valores válidos en formato pero no conectan.
spring.data.mongodb.uri=mongodb://localhost:27017/test-db

spring.ai.openai.api-key=dummy-key-for-tests
spring.ai.openai.base-url=https://api.groq.com/openai

jwt.secret=test-secret-key-that-is-long-enough-for-hmac-sha256-algorithm
jwt.expiration=86400000

fcm.enabled=false

aws.s3.access-key=dummy
aws.s3.secret-key=dummy
aws.s3.region=us-east-1
aws.s3.bucket=dummy-bucket
